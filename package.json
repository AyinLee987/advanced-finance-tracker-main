{
  "name": "advanced-finance-tracker",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "npx serve .",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^2.0.0",
    "vitest": "^2.0.0"
  }
}
